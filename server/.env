# Server Configuration
PORT=5001

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432 # Default PostgreSQL port
DB_USER=postgres # Or your dedicated app user
DB_PASSWORD=replacer123 # The password you set during install
DB_NAME=store_ratings_pg_db # The database you created

# Security
JWT_SECRET=thisisatemporarytestsecret12345 
BCRYPT_SALT_ROUNDS=10

# Frontend URL (for CORS in production)
# FRONTEND_URL=http://localhost:3000